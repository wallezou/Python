变量
数字
字符串
  
  单引号和双引号： '  "
  
  多行字符串:  """ '''
  
  改变大小写: 
    str.title() ——首字母大写
    str.upper() ——全部大写
    str.lower() ——全部小写
   
   连接字符串：str + str

格式化字符串简介：
.format()
var='str_{val1}{val2}'
print(var.format(val1=Hello,val2=Echo))
str_HelloEcho
  空白字符：\n,\t
  
 去除空白符：
 str.lstrip()
 str.rstrip()
 str.strip()
 
列表（Lists）：
列表也是一个变量

  命名和定义列表：
  列表是元素的集合，列表名最好设置为复数
  lists=['val1','val2','va1n']

  访问列表元素：
    lists=['val1','val2','valn']
    print(lists[0],lists[-1])
    
    
    
